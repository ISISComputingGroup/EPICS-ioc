record(ao, "$(P)$(CTRL):SP")
{
    field(DTYP, "stream")
    field(OUT, "@devtriton.proto set$(CTRL)($(P)) $(PORT)")
	field(PREC, "3")
}

record(ai, "$(P)$(CTRL)")
{
    field(DTYP, "stream")
    field(INP, "@devtriton.proto get$(CTRL)($(P)) $(PORT)")
    field(SCAN, "1 second")
    field(PREC, "3")	
}

